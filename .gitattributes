# Ensure shell scripts always use LF line endings (Windows → Linux Docker)
*.sh    text eol=lf

# Source files — LF
*.cpp   text eol=lf
*.h     text eol=lf
*.conf  text eol=lf

# Windows-native files — CRLF allowed
*.bat   text eol=crlf
*.ps1   text eol=crlf
